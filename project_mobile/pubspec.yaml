name: mobile_project
description: "A new Flutter project."

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.6.1
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  syncfusion_flutter_gauges: ^28.2.9
  http: ^1.3.0
  provider: ^6.1.2
  dio: ^5.8.0+1
  flutter_secure_storage: ^9.2.4
  jwt_decoder: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/realmush.png
    - assets/images/mushroom.png
